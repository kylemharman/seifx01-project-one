<%= simple_form_for( [@post, @post.comments.build], html: { class: 'form-group' }) do |f| %>
    <%= f.input :content, placeholder: "add comment" %>
    <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>